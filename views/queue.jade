extends layout

block content
    h1 Current Commands
    if commands
        - each command in commands
            p
                = JSON.stringify(command)
    else
       p
           No Commands Added Yet
